# Optional heavier ML stack for StableVITON integration.
# Install with: pip install -r requirements-ml.txt
# NOTE: Do NOT install torch/torchvision/torchaudio here. We rely on the
# base image's CUDA-matched PyTorch (e.g., torch 2.3.1+cu121). Installing
# different wheels here can break extensions (e.g., torch._C).
diffusers==0.29.1
transformers==4.41.2
accelerate==0.31.0
opencv-python-headless==4.9.0.80
numpy==1.26.4
safetensors==0.4.3
controlnet-aux==0.0.9
mediapipe==0.10.14
# xformers is optional; comment out to reduce wheel size/conflicts
xformers==0.0.27
scipy==1.12.0
einops==0.8.0
huggingface-hub==0.23.5
gdown==5.2.0
onnx==1.16.1
# Use CPU onnxruntime to avoid bundling mismatched CUDA libs
onnxruntime==1.16.2
gradio==4.44.0
